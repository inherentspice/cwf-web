import React from "react";
import { Button } from "@mui/material";

export default function SideBar() {
  return (
    <div className="sidebar">
      <h1>Sidebar</h1>
      <Button variant="contained" color="primary">Let's Button</Button>
    </div>
  )
}
